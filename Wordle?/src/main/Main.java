package main;

import java.io.IOException;
import java.util.Scanner;
import gui.Frame;

public class Main {
	public static void main(String args[]) throws IOException, InterruptedException {
		/*
		Wordle game = new Wordle();
		Scanner in = new Scanner(System.in);
		System.out.println("[1] Start game\n[2] Add words\n[3] Exit");
		
		int opt = in.nextInt();
		
		if(opt==1) game.start();
		else if(opt==2) {
			String i;
			System.out.println("Enter words line by line. Type -1 to exit");
			while( !(i=in.nextLine()).equals("-1") ) game.addWords(i);
		} else if(opt==3);
		else System.out.println(opt+" is not a valid option");
		
		in.close();
		*/
		
		Frame f = new Frame();
	}
}
